pandas>=2.0
numpy>=1.26
matplotlib>=3.7
seaborn>=0.13
scipy>=1.11
statsmodels>=0.14

# Jupyter kernel only — VS Code's Jupyter extension renders notebooks,
# no need for full `jupyter` + `notebook` meta-packages (those trigger
# Windows long-path errors at C:\...\venv\share\jupyter\labextensions\...)
ipykernel>=6.29

requests>=2.31

streamlit>=1.32
folium>=0.15
streamlit-folium>=0.18
plotly>=5.18

python-dotenv>=1.0
anthropic>=0.40

# Tests + Präsentation/Build
pytest>=8.0
markdown-pdf>=1.3        # presentation/*.md -> PDF (build_pdf.py)
python-pptx>=1.0         # PowerPoint-Foliendeck (build_pptx.py)
# playwright>=1.40       # optional: Webapp-Screenshots (capture_webapp_screenshots.py)
